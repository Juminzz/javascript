유전자 추가도 가능 (프로토 타입)
속성과 메소드등을 사용할수도 있음
JS = 객체 !!!!!!!!!!!!!!!!!!!!!
객체 !== 기본자료형
기본 배정받은 속성만 사용할수 있음 (예) length,toFix()...)
기본자료형 : 숫자,문자,불타입 속성추가 x
속성을 추가하고싶다 => 객체로 변환(Object)
숫자 => new Number()
문자 => new String()
불타입 => new Boolean()
const 객체 = new 객체 자료형 이름()

객체 { name: '홍길동', age: 30 } 배열은 객체다
JSON ( "name": "홍길동", "age": "30" )
JSON은 절대 주석 넣으면 안됨 각각 " " 가 들어감

함수 : function
객체 : Object
속성 : Property
메소드 : method (객체 내 함수)

CRUD
생성
읽기
수정
삭제

호이스팅 : 함수의 선언이 상단으로 끌어올려져
먼저 값이 들어가지 않고 상단에서 초기화되는 현상

호이스팅이 되지않는 상태는
함수를 선언과 동시에 생성할때
const = function (){} // 호이스팅 안됨
